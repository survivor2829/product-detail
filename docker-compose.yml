services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      PYTHONUNBUFFERED: "1"
    volumes:
      - uploads:/app/static/uploads
      - outputs:/app/static/outputs
    restart: unless-stopped

volumes:
  uploads:
  outputs:
