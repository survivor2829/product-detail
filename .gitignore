# 环境变量（含 API Key，绝对不能提交）
.env

# 数据库文件
*.db
instance/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# 运行时目录（用户上传/导出/临时）
output/
uploads/
static/uploads/
static/outputs/

# 参考图片（不属于代码）
static/设备类/ref_*.png

# 根目录临时图片/截图
*.png
*.jpg
*.zip

# 允许通用场景图片 (scene_bank 含子目录 water/pipeline/industrial/commercial)
!static/scenes/*.jpg
!static/scene_bank/*.jpg
!static/scene_bank/**/*.jpg
!static/scene_bank/manifest.json

# 工具生成文件
.omc/
PROJECT_ARCHITECTURE.md

# 调试 / 探针目录(下载脚本已迁到 scripts/download_scene_bank.py)
.probe/

# 批量生成的本地测试素材(zip 已足够做 E2E 夹具，解压目录不进仓库)
test_batch_input/

# v2 smoke / hand-test 输出（本地 WIP，不进仓库）
smoke_output_v2/
v2_result/

# v2 pipeline 任务运行时输出（每次 start_task 生成 v2_<ts>_<hex> 子目录,
# 含 block_*.jpg / assembled.png / _summary.json / _planning.json — 不是代码资产）
static/ai_refine_v2/

# 阶段一·任务 1.3 评审产物 (跑 scripts/stage1_planner_eval.py 真调 DeepSeek 的输出快照,
# 含 _planning_v2.json / _meta.json / _input_text.txt / _summary.md — 重跑可重新生成)
stage1_eval_output/

# 阶段五真测产物 (PRD §阶段五三关阶梯式真测烧 ¥12.66 累计的输出, 含 step1/step2/step2v2_real
# /step3_v4plan/step3_v4real/step3_full/step3_specfix 等子目录, 每个含 jpg + meta.json
# + raw_url 救图链接 + assembled.png. 是阶段五一次性真测产物, 不进仓库, 重做要重新烧钱)
stage5_real_test/

# 阶段六 PRD v3.1 真测产物 (跟 stage5 同款规则, PRD §9.7 硬要求)
stage6_real_test/

# 阶段六真调跑日志 (临时, 1 周监控期后清)
stage6_*.log
app_dev.log

# 系统文件
.DS_Store
Thumbs.db

# Claude Code 本地设置 (settings.local.json 等不进 git)
# 只忽略具体文件, 不忽略整个目录, 让 AUTONOMY.md 进 git
.claude/settings.local.json
.claude/scheduled_tasks.lock
