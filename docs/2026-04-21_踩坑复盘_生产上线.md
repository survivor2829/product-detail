# 2026-04-21 踩坑复盘 · 阶段六生产上线

> 归档日期：2026-04-21
> 覆盖范围：腾讯云首次生产部署（SSH 配置 / Docker 镜像构建 / Alembic 接管老 SQLite）
> 交叉引用：`PROJECT_STATUS_批量生成.md` → "技术债 / 经验教训" 新增 3 条铁律

---

## 背景

阶段六收官，上腾讯云 Ubuntu 24.04（2核2G，单机 SQLite + 内存 pub/sub，PG/Redis 延后到阶段七）。
路线图看起来一马平川：SSH 上去 → backup → git pull → docker build → docker up → 跑 migration → 重置密码 → 完工。

实际路上踩了 3 个大坑，每个都足够让单次部署从 15 分钟变成 2 小时。核心教训：
**默认从国外 CDN 下载的依赖 + 默认按全新环境建表的 ORM，在中国大陆 + 存量生产 DB 面前会串联爆炸。**

---

## 坑 1：Playwright chromium 首次安装卡死 40 分钟零字节

### 症状

`docker compose build web` 跑到 `playwright install chromium` 这一步。日志冻在
"Downloading Chrome for Testing 145.0.7632.6 from https://playwright.azureedge.net/..."
40 分钟后仍零字节，BuildKit work dir 仅增 11M（都是解压前 5% 的 zip 头），外部 TCP 连接数为 0。

跑 `curl -I https://playwright.azureedge.net/` 从同一台服务器：HTTP 307，0.66 秒。
**网络本身通，但 playwright 的 node downloader 走 Azure CDN 大文件首次 TLS 握手 + backoff 死循环。**

### 根因

1. `pip install playwright` 走的是 pypi（已配腾讯云镜像），这部分没事
2. `playwright install chromium` 后续拉的 **150MB Chrome 二进制** 走的是 `playwright.azureedge.net`
3. 中国大陆访问 Azure Edge CDN 的大文件握手有时会卡在非常长的 backoff，不会超时、不会报错、不出任何进度

### 修法（迭代 4 次才稳）

**Commit 004cd51** — 第一次尝试：加 `PLAYWRIGHT_DOWNLOAD_HOST=https://npmmirror.com/mirrors/playwright`
→ **立刻 404**。npmmirror 的 OSS bucket key 是 `binaries/playwright/...` 而不是 `mirrors/playwright/...`，URL 写错了。
这次失败 10 秒，总比卡 40 分钟强。

**Commit 4ebbe94** — 第二次：改用 `https://cdn.npmmirror.com/binaries/chrome-for-testing`
→ Chrome + chrome-headless-shell（167M + 116M）下载成功（200 秒，8MB/s 稳）！
但 playwright 1.58 的 `install chromium` 会自动拉 ffmpeg 作依赖，ffmpeg 的 URL 结构完全不同：
`{HOST}/builds/ffmpeg/1011/ffmpeg-linux.zip` → `https://cdn.npmmirror.com/binaries/chrome-for-testing/builds/ffmpeg/...` → 404。
单个 DOWNLOAD_HOST 覆盖不了两个前缀。

**Commit 7fd3d4e** — 第三次：拆两阶段 RUN，`chromium 先 ffmpeg 后`，第一阶段 `|| true` 吃掉 ffmpeg 404
→ 依然失败。playwright 的 `INSTALLATION_COMPLETE` marker 是"整个 install 命令走完才落盘"，
第一阶段 ffmpeg 失败 → 整个命令非零退出 → chrome+shell 的 marker 没写 → 第二阶段换 HOST 重跑时又试图下 chrome-headless-shell → 404。

**Commit 19faf7b** — 第四次：反转顺序 `先 ffmpeg 后 chromium`
→ ✅ 成功。ffmpeg 2MB 秒装 + 独立 marker 落盘 → chromium 阶段看到 ffmpeg marker 已完成直接跳过 → chrome+shell 从 CfT 镜像下载 → 全绿。

### 立碑 —— 中国部署三铁律

**铁律 1**：凡是"pip/npm 装完再自动调第二个 downloader"的工具（playwright / puppeteer / electron-builder / node-gyp），都**必须额外配国内镜像**。pypi 镜像只管 Python 包，二进制资源是独立的下载链路。

**铁律 2**：单个 `PLAYWRIGHT_DOWNLOAD_HOST` 覆盖不了 `chrome-for-testing` + `ffmpeg` 两条路径。如果必须从同一镜像源拉两类资源，**分两次 RUN，每次改 env**。顺序关键：先装"小+单独"的（ffmpeg 2MB），后装"大+批量"的（chromium 167MB）。反过来大文件先跑、小文件炸掉会把大文件的 marker 带走。

**铁律 3**：失败快是好事，失败慢是噩梦。一旦看到下载卡住**超过 90 秒没进度 + 外部 TCP=0**，立刻 kill、换镜像，别等。40 分钟卡死的教训：一次浪费的时间够我迭代 8 次镜像配置。

### 锁死手段

Dockerfile 注释把"为什么要这么分两阶段"写成完整段落（见 commit `19faf7b` 的 11 行注释）。
下次改动者必须读懂注释才不会"优化"回单 RUN。

---

## 坑 2：Alembic 遇上 `db.create_all()` 老 SQLite 的双重初始化

### 症状

新镜像第一次启动后跑 `docker compose exec web flask db upgrade`：
```
sqlite3.OperationalError: table users already exists
  File "/app/migrations/versions/a73747e2b475_baseline_schema_from_models.py", line 21
    op.create_table('users', ...)
```

查一下 `alembic_version` 表：存在，但**内容为空**。
查一下实际表：`['alembic_version', 'batch_items', 'batches', 'generation_logs', 'users']` 全都有。

### 根因

老部署跑的 app.py 启动时有 `db.create_all()` 兜底，会把所有 model 定义的表创出来（不走 Alembic）。
升到新代码后，Alembic 的 baseline migration 又要从零 `create_table('users', ...)` —— 表已经存在，炸。
`alembic_version` 表是 Alembic 启动时自动建的容器，内容（`version_num` 列）才是 "哪个 migration 已跑" 的记录。空 = "我什么都没跑过"。

### 修法

```bash
flask db stamp a73747e2b475
```

`stamp` 告诉 Alembic "这个版本号当作已应用、不要真跑 upgrade SQL"。表结构已经 ok（从 `create_all` 来的和 baseline migration 生成的列完全一致，因为两者都从同一个 models.py 出），**只差一行 version 记录**。

### 立碑 —— Alembic 上线铁律

**铁律 4**：从 `db.create_all()` 过渡到 Alembic 的第一次部署，**必须 stamp baseline 而不是 upgrade**。判断条件：
- 目标表已存在（`inspect(db.engine).get_table_names()` 有它们）
- `alembic_version` 表 version_num 为空
→ 跑 `flask db stamp <baseline_revision>`，一次性补记录，不动表。

**铁律 5**：未来 models 加字段走 Alembic migration 时，**先确认 `db.create_all()` 已从启动流程里删掉**，否则新增列可能被两边都想创建，导致冲突或"哪边赢"不确定。
当前 app.py 还保留 `db.create_all()` 做 SQLite 开发兜底 —— 生产用 Postgres + migration 后，这个兜底要删或加"仅 dev 环境"判断。

---

## 坑 3：SSH 免密卡在 authorized_keys 格式错误

### 症状

第一次从本地 Windows SSH 到腾讯云：
```
Permission denied (publickey)
```

但公钥已经贴进 `/root/.ssh/authorized_keys`，`sshd -T | grep publickey` 显示 `pubkeyauthentication yes`，`chmod 600` 到位，本地 `~/.ssh/config` 也配了。

### 根因

通过腾讯云 Web 终端粘贴公钥时，heredoc 解析问题导致 `authorized_keys` 里公钥前面多了一段提示文字，使得一行变成：
```
# ... (复制下面的公钥...)ssh-ed25519 AAAAC3... user@host
```
sshd 解析时把整行当一个 key，前缀不是合法 key 类型 → 认不出 → 拒绝。

### 修法

用单引号 echo 一行命令重写，绕开 heredoc 变量展开：
```bash
echo 'ssh-ed25519 AAAAC3... user@host' > ~/.ssh/authorized_keys
```

### 立碑 —— SSH 配置排查三板斧

**铁律 6**：配完 SSH 免密第一次登录失败，按顺序查：
1. **`cat authorized_keys` 看 raw bytes**：公钥必须一行，开头必须严格匹配 `ssh-` 或 `ecdsa-` 或 `sk-ssh-`。任何前缀注释会让整行失效。Web 终端粘贴时最容易带脏字符。
2. **`sshd -T | grep -E 'publickey|authorized'`**：确认 sshd 真的启用了 publickey 且读的是预期路径。别信 `/etc/ssh/sshd_config` 里的注释状态 —— `sshd -T` 是真的生效值。
3. **`ls -la ~/.ssh/ && ls -la ~/`**：权限必须 `700 ~/.ssh`，`600 authorized_keys`，`~/` owner 必须是目标用户。任何一处给 world-writable 都会被 sshd 拒绝。

**立即写 `~/.ssh/config` 起别名**：把 `Host <alias>` + `HostName <ip>` + `IdentityFile` 配好，之后所有命令走别名（`ssh tencent-prod` 比 `ssh root@124.221.23.173 -i ~/.ssh/id_ed25519` 省时省错）。Windows 下用 PowerShell `Out-File -Encoding ascii` 避免 UTF-8 BOM。

---

## 坑 4 (遗留，未处理，仅归档)：sshd PasswordAuthentication yes

### 现状

腾讯云默认 `/etc/ssh/sshd_config` 里 `PasswordAuthentication yes`。密钥登录已能工作，但密码登录通道仍开着 —— 意味着攻击者可以无限尝试 root 密码爆破。

### 未立即收紧的原因

本次部署主线是"让服务跑起来"，不想在半夜动 sshd 把自己锁在门外。密钥登录验证通过、`~/.ssh/config` 别名可用、重启 docker 不依赖 ssh 密码 —— 收紧是低优先级、可延后的硬化动作。

### 待办（阶段七头几天做）

1. 本地再开一个 ssh 会话保持连接（防锁门）
2. `sed -i 's/^#\?PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config`
3. `systemctl restart ssh`
4. 在保持连接的会话里 `exit` + 新开会话验证仍能免密登录
5. 若失败：在保持连接的会话里改回 `yes` + 重启 ssh

---

## 坑 5：Admin 密码重置"假成功" —— 写入后未验证就报告

### 症状

2026-04-21 晚第一次部署上线后, 我口头汇报 admin 密码已重置为
`^tMm6uC7qPyZ7sRN27M=`(当场明文交付用户)。用户当晚测试:
- 新密码登录 → 失败(返回登录页)
- 旧密码登录 → 成功(仍然能登录!)

→ **"重置"从未真正生效**, 但被当成成功上报。

### 根因

上一轮执行密码重置的步骤里, 只做了"写" 没做"读回验证":
1. 进 `flask shell` / `docker exec python` 调 `admin.set_password(new_pw); db.session.commit()`
2. 看到 `commit()` 没抛异常 → 就认为成功
3. 没有任何后续"读回 hash + check_password_hash(hash, new_pw)" 的校验步骤
4. 没有任何 "curl /auth/login 用新密码 POST, 看是不是 302" 的端到端验证

至于为什么 `commit()` 没抛异常但新密码不生效 —— **无法事后定位**,
因为 bash 历史 + 本地终端 scrollback 都没留痕。最有可能的嫌疑:
a) 我手敲密码时输错一个字符, 写进去的 hash 对应另一个字符串, 但我上报给用户时写的是"应该输入的那个"
b) `docker exec` 进的 python session 被 sandbox 拦截中途, 打的 "ok" 是部分输出
c) set_password 成功但事务被 `rollback` (container healthcheck 中途踢掉 session)

**但根本问题不是上面任何一个**: 是**写完没立刻证伪**, 所以哪个失败模式都会静默通过。

### 修法(2026-04-21 本次彻底修)

新建 `scripts/_tmp_reset_admin.py` (用完即删的一次性脚本), 强制 write+verify 闭环:

```python
# 1. 生成 16 位随机密码 (lower+upper+digit+special 四类齐全)
pw = generate()
# 2. 写入
u.set_password(pw); db.session.commit()
# 3. 强制从 DB 读回 (expire_all 防 SQLAlchemy identity map 假命中)
db.session.expire_all()
u2 = User.query.filter_by(username="admin").first()
# 4. 当场 check_password_hash —— 只有这一步 True 才算真的成功
verified = check_password_hash(u2.password_hash, pw)
print(f"VERIFIED={verified}\nPW={pw}")
```

`VERIFIED=False` → 立刻 exit 非零, 绝不把密码交付给用户。

随后在 shell 里再做端到端验证(3 curl):
```
POST /auth/login 用新密码   → 要 302
GET  /         带 session   → 要 200 + 首页里有 "批量生成" 关键字
GET  /batch/upload         → 要 200
```

本次全部通过(localhost 和公网 IP 双路径都验过), 才把新密码交付用户。

### 预防(立刻采纳)

**铁律:任何修改生产数据的命令, 写完必须紧跟一步"读回验证",
并且验证目标必须是"功能维度"(能不能登录), 不是"存储维度"(有没有写)。**

对应具体场景:
| 写操作 | 错误验证(只看存储) | 正确验证(看功能) |
|---|---|---|
| 重置密码 | `commit()` 不抛异常 | `check_password_hash(new_hash, pw)` + 用新密码 curl 登录 |
| 更新 user flag | `UPDATE` row count = 1 | 以该用户身份请求受该 flag 限制的路由, 看通过与否 |
| alembic upgrade | `alembic_version` 表有行 | 启动服务 + 调一个用到新字段的 API, 看响应正常 |
| 改模板 | `grep 新关键字 文件名` | `curl 页面 + grep 新关键字 响应` |

---

## 坑 6：首页缺批量生成入口 —— 孤儿模板 + Deploy skill 只 restart 不 build

### 症状

用户 Ctrl+F5 硬刷后, 登录态正常但顶栏看不到"批量生成"链接。
直接访问 `/batch/upload` → 200 能打开。
即路由存在、功能存在、**首页就是没入口**。

### 根因(两层复合)

**第一层 —— 改错了模板文件**:
- `/` 路由在 `app.py:1042` 渲染的是 `render_template("workspace.html")`, **不是** `index.html`
- 但 `templates/index.html:155` 有 `<a href="/batch/upload">批量生成</a>`
- `templates/workspace.html:430` 的 `.topbar-right` 没这个链接
- `grep -r "render_template.*index.html" app.py` → **零结果**: `index.html` 是**孤儿模板**, 从未被渲染
- 之前提 PR "加批量生成入口" 时, 有人(可能是上一个 session 的我)把链接加到了 `index.html`, 看到"文件有这个字符串"就认为完事, 没验证页面真的渲染出来了

**第二层 —— Deploy skill 只 restart 不 build, 所以就算改对了文件也不部署**:
- `docker-compose.yml` 里 `templates/` 是靠 Dockerfile `COPY . .` 烤进镜像的, **不是** bind mount
- 现有 `/deploy` skill 只跑 `docker compose restart`, 重启容器用的是**老镜像**的模板
- 如果改的是 `workspace.html`, 即使 `git pull` 成功, `restart` 也完全不会生效
- 所以"改了代码 → git pull → 用户还是看不到"这个 bug 会**同时具备**上述两个根因特征

确认证据:
- 改 bug 前: `grep -c 批量生成 templates/index.html` (server) = 1 ✅ (repo 有)
- 改 bug 前: `grep -c 批量生成 /app/templates/index.html` (container) = 1 ✅ (镜像也有)
- 改 bug 前: `grep -c 批量生成 templates/workspace.html` (server) = **0** ❌
- 改 bug 前: `grep -c 批量生成 /app/templates/workspace.html` (container) = **0** ❌

→ 整条链从模板到部署都干净, 只是改了个错文件。

### 修法

1. `templates/workspace.html:430` 在 `.topbar-right` 最左侧加两个 link:
   ```html
   <a href="/batch/upload">批量生成</a>
   <a href="/batch/history">历史记录</a>
   ```
   `/batch/history` 路由在 `app.py:1121` (原 PRD 任务6, 跟着一起曝光)。
2. **必须 rebuild 镜像**:
   ```bash
   git pull
   docker compose build web   # 缓存命中率高, 只有 COPY . . 那层重跑 (<1s)
   docker compose up -d web   # recreate container from new image
   ```
3. curl 验证: `GET /` 响应 grep "批量生成" 必须有命中。

### 预防

1. **删除或明确 deprecate `templates/index.html`**(阶段七做, 本次不动):
   它现在是**纯负债**, 存在仅会让下次维护者再次改错文件。
   要么删掉, 要么改名 `templates/index_legacy_wubaoyun.html`。
2. **/deploy skill 必须升级**:
   - 当前: `git pull && docker compose restart`
   - 升级后: `git pull`; 然后判断 `git diff HEAD@{1} HEAD --name-only` 里
     有没有 `Dockerfile`/`requirements.txt`/`templates/`/静态资源 → 有就 `docker compose build web && up -d web`, 没有才 `restart`
   - (今晚不改 skill, 先在 CLAUDE.md 挂一条备忘提醒, 下次改 skill 一并做)
3. **模板类变更的验证口径**: 不看"文件有这个字符串", 看
   `curl --cookie <登录态> $URL | grep <字符串>` —— 真有响应里才算数。

---

## 横向教训（跨 6 个坑的共性）

### 1. "看起来能跑" ≠ "真的能跑"

- `curl -I https://playwright.azureedge.net/` 返回 307，**网络通**。但大文件下载会卡死 —— 握手通不代表带宽通。
- `alembic_version` 表存在，**Alembic 初始化了**。但内容为空 —— 存在不代表记录。
- 公钥贴进了 authorized_keys，**文件存在**。但多了前缀 —— 存在不代表格式对。

**判据换一遍**：别看"是不是有"，看"是不是按预期工作"。对每一个环节问"我怎么证明它真的在按预期工作"，答案必须是命令 + 预期输出的组合。

### 2. 中国大陆部署的"默认国外资源"陷阱

Docker / Python / Node 工具链默认从海外拉东西，在国内都得过一层镜像：
- `apt` → 腾讯云/阿里云镜像（已配）
- `pip` → 腾讯云/清华镜像（已配）
- `playwright install` → npmmirror CfT（这次踩坑补上）
- `docker pull`（基础镜像）→ 可选配置 `registry-mirrors`（腾讯云节点已经够快，暂不换）
- rembg model（`.onnx` 文件）→ 已用 `ghfast.top` 镜像前置加速

**凡是有独立 downloader 的工具**：`playwright`、`puppeteer`、`electron`、`chromedriver-manager`、`selenium-manager`、`browsers.json` 风格的任何东西 → **都得独立配镜像**。别指望一个顶层 env var 解决全部。

### 3. 所有写操作必须"闭环到功能验证"，不能停在"存储层成功"

坑 5 (密码重置假成功) 和 坑 6 (模板改错文件 + deploy skill 只 restart) 本质是同一个反模式: **"写" 完了就当"成功"**, 没有写→读→功能性验证的闭环。

| 错误验证口径 | 什么场景会骗过 | 正确验证 |
|---|---|---|
| `commit()` 不抛异常 | 写的值跟你以为的值不一致; 事务被别处 rollback | 读回 + check_password_hash / SELECT 比对 |
| `grep 关键字 本地文件` | 文件对了但路由渲染的是另一个模板 | curl 响应 + grep |
| `flask db upgrade` 退出 0 | 没有新版本要升; 或错把空 alembic_version 当作"已 baseline" | 启动服务 + 调一个用新 schema 的 API |
| `docker compose restart` 之后 `ps` 显示 Up | 用的还是老镜像, 新代码没进去 | curl 访问改动点, grep 新行为 |
| `git push` 成功 | 改动文件不是 `/` 渲染的那个 | 打开目标 URL 实际看 |

**一刀切铁律**: 写完 → 立即构造一次最小的"读或用"动作去证明写真的生效 → **对该最小动作显式 assert** (非零退出 / 日志红字 / CI 断言)。

### 4. 生产上线 + 存量数据库的 "第一次 migration" 必定出坑

ORM 从 `create_all` 过渡到 Alembic 几乎一定会遇到 baseline 冲突。下次类似升级的预检：
1. `inspect(db.engine).get_table_names()` → 列出所有表
2. `SELECT version_num FROM alembic_version` → 看是否为空
3. 对比 `migrations/versions/` 里的 baseline → 两边表集合是否一致
4. 一致 → `stamp baseline`；不一致 → 手工写 conditional migration 或 manual ALTER

---

## 本次部署最终结果

- **生产 URL**：http://124.221.23.173:5000/
- **容器**：`clean-industry-ai-assistant-web-1` Up（gunicorn 2 × gthread workers）
- **数据持久化**：named volumes `uploads` / `outputs` / `ai_cache` + bind-mount `instance/`
- **数据库**：SQLite 单机 `/app/instance/wubaoyun.db`，schema 从 `db.create_all` 过来，alembic 已 stamp baseline
- **镜像源全链路国内化**：apt 腾讯云、pip 腾讯云、playwright npmmirror CfT、rembg model ghfast.top
- **关键 commits**：
  - `004cd51` → `4ebbe94` → `7fd3d4e` → `19faf7b`（4 次 Dockerfile 迭代才稳）
  - `591384a` 首轮收尾文档
  - `26654a9` /simplify 清理(Dockerfile 注释去重 + .dockerignore 扩)
  - `833a1ee` **修 坑 6**: workspace.html topbar 补批量生成/历史记录入口
  - 坑 5/6 修复后二次上线(见本文档最后一次 commit)
- **二次上线(坑 5+6 修复后)**:
  - 密码已重置为 16 位随机强密码, 通过 write+verify+curl-login 三重确认
  - 3 个 curl 端到端测试全绿 (localhost + 公网 IP 两路径):
    - `POST /auth/login` → 302 + session cookie
    - `GET /` → 200 + 首页有 `批量生成` + `/batch/upload`
    - `GET /batch/upload` → 200 (59101 字节)
- **已知待办（阶段七）**：
  - sshd PasswordAuthentication 收紧
  - PG + Redis 上大机（2核2G 撑不起全栈）
  - `db.create_all()` 生产环境兜底逻辑精简（仅保留 dev 环境）
  - **新增**: 删除/重命名孤儿 `templates/index.html` (坑 6 根因)
  - **新增**: `/deploy` skill 升级 — 检测 Dockerfile/templates 变更自动加 `build --no-cache`

---

## 阶段七（硬件升级后）待办预排

1. **机器升级 4C8G 起**：现 2C2G 跑 gunicorn 2 workers + 首次 rembg 模型加载会偶发 OOM
2. **Postgres + Redis 接入**：
   - `.env` 改 `DATABASE_URL=postgresql://...` + `PUBSUB_BACKEND=redis`
   - `docker compose --profile full up -d`
   - `scripts/migrate_sqlite_to_pg.py --commit` 把 SQLite 数据迁过去
3. **sshd 硬化**：PasswordAuthentication no + PermitRootLogin prohibit-password
4. **备份自动化**：cron daily backup SQLite/PG + uploads tar.gz → 对象存储
5. **监控**：docker compose logs 接一层 promtail/loki 或至少 logrotate 防磁盘满
