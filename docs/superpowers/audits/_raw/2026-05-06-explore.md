### §D.1 未使用的提示函数对（ai_image.py / ai_image_volcengine.py）
- **位置**: `ai_image.py:178-198` / `ai_image_volcengine.py:225-245`
- **严重度**: 低
- **死亡证据**: `prompt_comparison_bg()` 和 `prompt_brand_bg()` 定义在两个文件中, 全仓 grep 结果为 0 调用（仅定义处）
- **死亡时间估算**: 创建于 2026-04-23（test_deepseek_planner.py 同期）, 代码注释显示为"对比屏"和"品牌屏"但从未被 generate_background / generate_detail_backgrounds 路由调用
- **建议处置**: 归档到 scripts/archive/unused_prompts.py (保留以备未来扩展, 因这两个提示结构完整)
- **风险**: 低风险，纯粹辅助函数

### §D.2 重复实现的 _to_data_url 函数
- **位置**: `ai_bg_cache.py:38-70` vs `ai_refine_v2/refine_generator.py:239-285`
- **严重度**: 中
- **死亡证据**: 两套实现并行存在。ai_bg_cache 版本无 resize 兜底（仅透传/base64）; ai_refine_v2 版本有 max_bytes/max_dim 防 413 逻辑（memory v3.2.2 提到"33MB→480KB"）。调用链：ai_bg_cache 版被 ai_bg_cache 自己 + scripts/smoke_test_refine_v2.py 调用; ai_refine_v2 版被 ai_refine_v2/refine_generator.py + tests/* 调用
- **死亡时间估算**: ai_bg_cache 版创建更早（背景缓存模块），ai_refine_v2 版为 2026-04-24（新管线重构）
- **建议处置**: 旧的 ai_bg_cache._to_data_url 可归档，ai_refine_v2 版本功能更完整（含 resize 保护）。长期：统一到单个工具模块
- **风险**: 中等。假如两版本参数演变不同步，会导致边界条件处理不一致

### §D.3 未调用的演示/测试脚本集合
- **位置**: `scripts/` 目录下 6 个脚本
  - `demo_refine_v2.py` (2026-04-24)
  - `smoke_pipeline_schema.py` (2026-04-27)
  - `smoke_test_refine_v2.py` (2026-04-24)
  - `stage1_planner_eval.py` (2026-04-27)
  - `test_deepseek_planner.py` (2026-04-23)
  - `refine_v1_with_gpt_image.py` (2026-04-24)
- **严重度**: 中
- **死亡证据**: 全仓无 import 或调用；均为独立可执行脚本（#!/usr/bin/env python 或 if __name__ == "__main__"）；创建日期集中在 2026-04-23~27 重构周期内（"阶段一完成" 标签）
- **死亡时间估算**: 10+ 天前（基于 feat(refine-v2) 提交日期），属"阶段性收尾脚本"
- **建议处置**: 归档到 `scripts/archive/refine-v2-stage1/` (保留以供历史查证和下次重构参考)
- **风险**: 低风险。未被导入，删除不影响运行时；保留便于追溯阶段一验收标准

### §D.4 未被直接渲染的 Jinja 模板（ai_compose）— NA
- **位置**: `templates/ai_compose/*.html` (8 个) 和 `templates/blocks/main_img_*.html` (5 个)
- **严重度**: 低（设计原因, 实际为 NA）
- **死亡证据**: 这些模板不在 Python render_template() 调用中，但通过 ai_compose_pipeline.py 的动态加载机制（env.get_template() + _registry.json）被使用。templates/ai_compose/_registry.json 注册了 hero/advantages/effect/specs/vs/brand/scene/cta，对应的 .html 由 render_screens() → _render_one() → env.get_template(template) 动态加载
- **死亡时间估算**: 结构设计完整（registry 模式），无过期迹象
- **建议处置**: 保留（设计合理，非 dead code）
- **风险**: 零。静态扫描工具会误判为"未引用"，但代码实际依赖

### §D.5 未使用的 .probe 目录
- **位置**: `.probe/` (4 个文件：pexels.html / wiki_street.html / test_street.jpg / wm_search.json)
- **严重度**: 低
- **死亡证据**: 全仓 grep 无任何引用；directory 无说明文档；文件时间戳 2026-04-15（早期实验阶段）
- **死亡时间估算**: 21+ 天前，属"场景图库 v2 迭代前的探针目录"
- **建议处置**: 归档到 `docs/archive/probe-experiment-v1/`（保留实验数据）或直接删除
- **风险**: 零。仅静态探针数据

### §D.6 未调用的辅助函数（app.py） — NA
- **位置**: `app.py:5024` (_kill_old_flask)
- **严重度**: 低 (实际为 NA — 在用)
- **死亡证据**: 定义 1 处，调用 1 处（脚本启动时 _kill_old_flask(5000)），为"一次性启动兜底"，非 dead code
- **建议处置**: 保留（有效用途：清理端口占用）

---

**总体评估**: 无严重阻碍性 dead code。主要堆积为"阶段性完成脚本"和"演进过程中的旧实现"。最有价值的归档候选是 demo/smoke/stage1/test 脚本组（释放 ~2.5K 行代码视觉噪音）和 ai_bg_cache._to_data_url 重复（统一工具函数降低维护成本）。
