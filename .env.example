# DeepSeek API Key（平台Key，付费用户和管理员使用）
DEEPSEEK_API_KEY=sk-你的key

# 本地代理（可选，有Clash等代理时填写）
HTTP_PROXY=http://127.0.0.1:7890

# Flask Session 密钥（生产环境必须更换为随机值）
# 生成方式: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-in-production

# API Key 加密密钥（用于加密存储用户自定义的 DeepSeek Key）
# 生成方式: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=生成一个key填这里

# 数据库地址（默认 SQLite，商用化可换 PostgreSQL）
# DATABASE_URL=sqlite:///wubaoyun.db
